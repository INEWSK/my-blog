<div class="hd posts">
    <a href="javascript:history.back()"><i class="fa fa-reply replay-btn" aria-hidden="true"></i></a>
    <div class="post-title">
        <p><%= page.title %></p>
        <hr />
    </div>
    <% if (page.layout == 'post')  {%>
    <div class="post-overdue-notice">
        <i class="fa-regular fa-bell notice-bell"></i>
        此文章內容的最後更新時間為
        <span class="day_num"></span>
        天前，部分信息可能已發生改變，請注意甄別
    </div>
    <% } %>
    <div class="post-content"><%- page.content %></div>

    <% if (theme.valine.enable && page.comments) { %>
    <hr class="fhr" />
    <div id="vcomments"></div>
    <% if (page.layout == 'post') { %>
    <!-- id 将作为查询条件 -->
    <span id="<%- url_for(page.path) %>" class="leancloud_visitors" data-flag-title="<%- page.title %>">
        <em class="post-meta-item-text">閱讀量 </em>
        <i class="leancloud-visitors-count">載入中...</i> 次
    </span>
    <% } %> <% } %>
    <script>
        const pub_date = "<%- page.date %>";
    </script>
</div>